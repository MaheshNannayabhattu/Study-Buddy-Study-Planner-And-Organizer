<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_studyplan"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_studyplan"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyPlannerNew"
        tools:targetApi="31">
        <activity
            android:name=".UniversityDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".TopSchoolsActivity"
            android:exported="false" />
        <activity
            android:name=".MyTaskActivity"
            android:exported="true" >

        </activity>
        <activity
            android:name=".AddTaskActivity"
            android:exported="false" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".SignInActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.StudyPlannerNew">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>